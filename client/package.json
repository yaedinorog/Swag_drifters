{
  "name": "client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "node ../scripts/sync-public-assets.mjs",
    "dev": "vite",
    "prebuild": "node ../scripts/sync-public-assets.mjs",
    "build": "tsc -p tsconfig.json && vite build",
    "postbuild": "node ../scripts/postbuild-editor-entry.mjs",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.8",
    "jsdom": "^24.1.3",
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vitest": "^2.1.8"
  }
}
